# Next.js local env example (do not commit secrets)
# Copy to .env.local and fill values for local development.

# UI/campus
NEXT_PUBLIC_LOCATION=lewiston

# Optional durable capacity path
# Used by API route to invoke the atomic reservation Lambda.
# Leave empty to use local per-slot lock fallback in dev.
# RESERVE_FUNCTION_NAME=reserve-registration

# Example: additional server-side secrets (leave unset if not used)
# RECAPTCHA_SECRET=your-secret-here
# SMTP_HOST=smtp.example.com
# SMTP_USER=local-user
# SMTP_PASS=local-pass

